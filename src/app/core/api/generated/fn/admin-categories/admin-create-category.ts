/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateCategoryCommand } from '../../models/create-category-command';
import { CreateCategoryResponse } from '../../models/create-category-response';

export interface AdminCreateCategory$Params {
      body: CreateCategoryCommand
}

export function adminCreateCategory(http: HttpClient, rootUrl: string, params: AdminCreateCategory$Params, context?: HttpContext): Observable<StrictHttpResponse<CreateCategoryResponse>> {
  const rb = new RequestBuilder(rootUrl, adminCreateCategory.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CreateCategoryResponse>;
    })
  );
}

adminCreateCategory.PATH = '/api/v1/admin/categories';
