/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaymentDetailDto } from '../../models/payment-detail-dto';

export interface GetPaymentByOrderId$Params {
  orderId: string;
}

export function getPaymentByOrderId(http: HttpClient, rootUrl: string, params: GetPaymentByOrderId$Params, context?: HttpContext): Observable<StrictHttpResponse<PaymentDetailDto>> {
  const rb = new RequestBuilder(rootUrl, getPaymentByOrderId.PATH, 'get');
  if (params) {
    rb.path('orderId', params.orderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaymentDetailDto>;
    })
  );
}

getPaymentByOrderId.PATH = '/api/v1/payments/{orderId}';
