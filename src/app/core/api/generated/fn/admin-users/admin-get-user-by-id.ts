/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserDetailDto } from '../../models/user-detail-dto';

export interface AdminGetUserById$Params {
  id: string;
}

export function adminGetUserById(http: HttpClient, rootUrl: string, params: AdminGetUserById$Params, context?: HttpContext): Observable<StrictHttpResponse<UserDetailDto>> {
  const rb = new RequestBuilder(rootUrl, adminGetUserById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserDetailDto>;
    })
  );
}

adminGetUserById.PATH = '/api/v1/admin/users/{id}';
