/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedResponseOfUserSummaryDto } from '../../models/paged-response-of-user-summary-dto';

export interface AdminGetUsers$Params {
  page_number?: (number | string);
  page_size?: (number | string);
  search?: string;
}

export function adminGetUsers(http: HttpClient, rootUrl: string, params?: AdminGetUsers$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfUserSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, adminGetUsers.PATH, 'get');
  if (params) {
    rb.query('page_number', params.page_number, {});
    rb.query('page_size', params.page_size, {});
    rb.query('search', params.search, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfUserSummaryDto>;
    })
  );
}

adminGetUsers.PATH = '/api/v1/admin/users';
