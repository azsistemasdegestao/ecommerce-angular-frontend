/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IFormFile } from '../../models/i-form-file';
import { UploadProductImageResponse } from '../../models/upload-product-image-response';

export interface UploadProductImage$Params {
  id: string;
      body: {
'file': IFormFile;
}
}

export function uploadProductImage(http: HttpClient, rootUrl: string, params: UploadProductImage$Params, context?: HttpContext): Observable<StrictHttpResponse<UploadProductImageResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadProductImage.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UploadProductImageResponse>;
    })
  );
}

uploadProductImage.PATH = '/api/v1/catalog/products/{id}/image';
