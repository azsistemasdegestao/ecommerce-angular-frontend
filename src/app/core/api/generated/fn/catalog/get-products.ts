/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedResponseOfProductSummaryDto } from '../../models/paged-response-of-product-summary-dto';

export interface GetProducts$Params {
  page_number?: (number | string);
  page_size?: (number | string);
  category_slug?: string;
  search?: string;
  min_price?: (number | string);
  max_price?: (number | string);
  in_stock?: boolean;
}

export function getProducts(http: HttpClient, rootUrl: string, params?: GetProducts$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfProductSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, getProducts.PATH, 'get');
  if (params) {
    rb.query('page_number', params.page_number, {});
    rb.query('page_size', params.page_size, {});
    rb.query('category_slug', params.category_slug, {});
    rb.query('search', params.search, {});
    rb.query('min_price', params.min_price, {});
    rb.query('max_price', params.max_price, {});
    rb.query('in_stock', params.in_stock, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfProductSummaryDto>;
    })
  );
}

getProducts.PATH = '/api/v1/catalog/products';
