/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RefundPaymentResponse } from '../../models/refund-payment-response';

export interface AdminRefundPayment$Params {
  id: string;
}

export function adminRefundPayment(http: HttpClient, rootUrl: string, params: AdminRefundPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<RefundPaymentResponse>> {
  const rb = new RequestBuilder(rootUrl, adminRefundPayment.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RefundPaymentResponse>;
    })
  );
}

adminRefundPayment.PATH = '/api/v1/admin/payments/{id}/refund';
