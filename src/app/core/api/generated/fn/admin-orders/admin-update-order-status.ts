/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateOrderStatusRequest } from '../../models/update-order-status-request';
import { UpdateOrderStatusResponse } from '../../models/update-order-status-response';

export interface AdminUpdateOrderStatus$Params {
  id: string;
      body: UpdateOrderStatusRequest
}

export function adminUpdateOrderStatus(http: HttpClient, rootUrl: string, params: AdminUpdateOrderStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<UpdateOrderStatusResponse>> {
  const rb = new RequestBuilder(rootUrl, adminUpdateOrderStatus.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UpdateOrderStatusResponse>;
    })
  );
}

adminUpdateOrderStatus.PATH = '/api/v1/admin/orders/{id}/status';
