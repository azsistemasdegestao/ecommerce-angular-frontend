/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedResponseOfAdminOrderSummaryDto } from '../../models/paged-response-of-admin-order-summary-dto';

export interface AdminGetAllOrders$Params {
  page_number?: (number | string);
  page_size?: (number | string);
  status?: string;
  user_id?: string;
}

export function adminGetAllOrders(http: HttpClient, rootUrl: string, params?: AdminGetAllOrders$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfAdminOrderSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, adminGetAllOrders.PATH, 'get');
  if (params) {
    rb.query('page_number', params.page_number, {});
    rb.query('page_size', params.page_size, {});
    rb.query('status', params.status, {});
    rb.query('user_id', params.user_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfAdminOrderSummaryDto>;
    })
  );
}

adminGetAllOrders.PATH = '/api/v1/admin/orders';
